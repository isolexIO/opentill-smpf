{
  "name": "DUCWalletSettings",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "description": "Optional description for search visibility and SEO (up to 1000 characters)",
      "maxLength": 1000
    },
    "verified_duc_mint": {
      "type": "string",
      "description": "Verified $DUC mainnet mint address (Solana public key). Null until an admin verifies it."
    },
    "previous_duc_mint": {
      "type": "string",
      "description": "Last verified mint before the most recent change (audit trail)"
    },
    "mint_changed_by": {
      "type": "string",
      "description": "Admin email that last changed the verified mint"
    },
    "mint_changed_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the verified mint was last changed"
    },
    "rpc_mainnet": {
      "type": "string",
      "default": "https://api.mainnet-beta.solana.com",
      "description": "Mainnet RPC endpoint"
    },
    "rpc_testnet": {
      "type": "string",
      "default": "https://api.testnet.solana.com",
      "description": "Testnet RPC endpoint"
    },
    "rpc_devnet": {
      "type": "string",
      "default": "https://api.devnet.solana.com",
      "description": "Devnet RPC endpoint"
    },
    "das_provider": {
      "type": "string",
      "description": "DAS-compatible NFT/token indexing provider URL"
    },
    "price_api_url": {
      "type": "string",
      "description": "Reputable price API base URL"
    },
    "duc_name": {
      "type": "string",
      "default": "Digital Utility Credit"
    },
    "duc_symbol": {
      "type": "string",
      "default": "DUC"
    },
    "duc_decimals": {
      "type": "number",
      "default": 6
    },
    "approved_chip_collections": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Admin-approved openTILL Chip NFT collection addresses"
    },
    "approved_token_mints": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Admin-approved/verified token mint addresses"
    },
    "default_network": {
      "type": "string",
      "enum": [
        "mainnet",
        "testnet",
        "devnet"
      ],
      "default": "mainnet",
      "description": "Active Solana cluster the wallet reads balances, tokens, and history from."
    },
    "is_paused": {
      "type": "boolean",
      "default": false,
      "description": "Global circuit breaker. When true, all SMPF wallet outgoing transfers and mint operations are blocked across the application."
    }
  },
  "required": [],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {},
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}